# Gateway environment example
# Copy this to .env (or set these in your deployment environment)

# Gateway listen port
PORT=5000

# Auth service URL (set per environment)
# Local development (gateway + auth running on same host):
# AUTH_SERVICE_URL=http://127.0.0.1:5001
# Docker Compose (services on same network, use service name):
# AUTH_SERVICE_URL=http://auth-service:5001
# Kubernetes (cluster DNS):
# AUTH_SERVICE_URL=http://auth-service:5001
# Production cloud (internal LB or private DNS):
# AUTH_SERVICE_URL=https://auth.internal.example.com

JWT_SECRET=dev-secret-key

# Redis for token blacklist (optional)
REDIS_HOST=redis
REDIS_PORT=6379

NODE_ENV=development
